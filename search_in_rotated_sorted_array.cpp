#include <iostream>
#include <vector>
#include <algorithm>

int main(){
    std::vector<int> nums = {4,5,6,7,0,1,2};
    int low = 0;
    int high = nums.size() - 1;
    int target = 0;

    while(low <= high){
        int mid = low + (low + high) / 2;
        if(nums[low] < )
    }

    return 0;
}



